# SQL Learning Game Frontend

A simple React + TypeScript frontend for the SQL learning game. It demonstrates a scenario with a textarea-based SQL editor and basic routing. Styling uses **Tailwind CSS** and components adapted from **shadcn/ui**. Forms are managed with **react-hook-form**.

## Available Scripts

- `npm start` - start a development build (requires webpack)
- `npm run build` - build for production
